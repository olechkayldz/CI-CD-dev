name: Cypress Projeleri

on:
  push:
    branches:
    - main

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
    - name: Kodu indir
      uses: actions/checkout@v4

    - name: Test runs
      uses: cypress-io/github-action@v5
